[project]
name = "innoagent"
version = "0.1.0"
description = "A minimal LangGraph-based general agent."
requires-python = ">=3.10"
dependencies = [
    "langgraph>=0.2,<1",
    "pydantic>=2,<3",
    "httpx>=0.25,<1",
]
