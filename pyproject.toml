[project]
name = "innoagent"
version = "0.1.0"
description = "An event-driven coding agent with tools, planning, reflection, and a terminal UI."
requires-python = ">=3.10"
dependencies = [
    "langgraph>=0.6,<1",
    "prompt-toolkit>=3.0,<4",
    "pydantic>=2,<3",
    "httpx>=0.25,<1",
    "PyYAML>=5.4,<7",
]
